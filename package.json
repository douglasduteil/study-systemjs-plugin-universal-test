{
  "name": "study-systemjs-plugin-universal-test",
  "version": "0.0.0",
  "description": ":microscope: Studying how to test SystemJS Plugins.",
  "main": "index.js",
  "scripts": {
    "build": "for D in packages/*/src; do [ -d \"${D}\" ] && babel $D -d $(echo $D | sed -e \"s/src/lib/g\"); done",
    "lint": "eslint packages",
    "prepublish": "npm run build",
    "preversion": "npm run build",
    "test": "npm run lint && npm run test:node",
    "test-cov": "echo \"Error: no test specified\" && exit 1",
    "test:browser": "npm run write-zuul && zuul test/browser.js --no-coverage",
    "test:node": "tap packages/*/test/*.js",
    "write-zuul": "COMMIT_SHA1=$(echo $TRAVIS_COMMIT | cut -c1-7 ) && printf \"name: $TRAVIS_BRANCH ($COMMIT_SHA1) - $TRAVIS_JOB_NUMBER\\nui: tape\\nbrowsers:\\n  - name: $BROWSER_NAME\\n    version: $BROWSER_VERSION\\n\">.zuul.yml"
  },
  "keywords": [],
  "author": "Douglas Duteil <douglasduteil@gmail.com> (douglasduteil.github.io)",
  "license": "WTFPL",
  "devDependencies": {
    "babel-cli": "^6.9.0",
    "babel-preset-es2015": "^6.9.0",
    "eslint": "^2.10.2",
    "eslint-config-standard": "^5.3.1",
    "eslint-plugin-promise": "^1.1.0",
    "eslint-plugin-standard": "^1.3.2",
    "tap": "^5.7.2",
    "zuul": "^3.10.1"
  },
  "dependencies": {
    "systemjs": "^0.19.29"
  }
}
